<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- لحفظ ملفات PDF/MP4/.kdp الناتجة في مجلد التنزيلات -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ✅ كشف تطبيقات التقاط الحزم (لحماية إضافية) -->
    <queries>
        <package android:name="com.guoshi.httpcanary" />
        <package android:name="com.guoshi.httpcanary.premium" />
        <package android:name="app.greyshirts.sslcapture" />
        <package android:name="com.emanuelef.remote_capture" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@android:drawable/sym_def_app_icon"
        android:theme="@style/Theme.KdpTool"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
