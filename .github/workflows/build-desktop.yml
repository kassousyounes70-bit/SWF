name: Build Windows Desktop EXE

on:
  workflow_dispatch:

jobs:
  build-tauri:
    permissions:
      contents: write
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Install Tauri CLI
        run: npm install @tauri-apps/cli@v1

      - name: Generate Official Tauri Icons
        run: |
          node -e "const fs = require('fs'); const buf = Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAEAAAABACAMAAACdtAo9AAAAA1BMVEUAAACnej3aAAAAEElEQVR42mNkGAWjYBSMAgBLAAGAAAGO0QAAAABJRU5ErkJggg==', 'base64'); fs.writeFileSync('src-tauri/app-icon.png', buf);"
          npx tauri icon src-tauri/app-icon.png

      - name: Build Tauri EXE directly
        run: npx tauri build

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: YK-PubEngine-Windows
          path: src-tauri/target/release/bundle/nsis/*.exe
